模拟器启动

cmd /c "cd tarotAI && npm start"

cmd /c "cd tarotAI && npm run android"

cmd /c "netstat -ano | findstr :8081"

cmd /c "cd tarotAI && npm start -- --reset-cache"

cmd /c "cd tarotAI && .\start-emulator.bat && npm run android"

cmd /c "cd tarotAI && .\start-emulator.bat && npm run android -- --no-install"

./tarot-admin-web && npm run build 

查进程：
netstat -ano | findstr :8081
tasklist | findstr 27620

杀进程：
taskkill /PID 27620 /F

cd my-tarot-app
npm run start

cd my-tarot-app && npm install --legacy-peer-deps


DEBug

  - $pkg = 'com.biiinnn.mytarotapp'                                  
  - $appPid = adb shell pidof -s $pkg                                
  - $appPid  确认能打印一个数字 
  - adb logcat -v time --pid 18162 


