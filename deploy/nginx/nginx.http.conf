worker_processes auto;
events { worker_connections 1024; }
http {
  sendfile on;
  gzip on;
  client_max_body_size 512m;

  upstream backend { server backend:8000; }
  upstream admin   { server admin:3000; }

  server {
    listen 80;
    server_name www.miidea.top miidea.top;
    client_max_body_size 512m;

    location /.well-known/acme-challenge/ {
      alias /var/www/certbot/.well-known/acme-challenge/;
      default_type text/plain;
      try_files $uri =404;
    }

    location = /health {
      proxy_pass http://backend;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api/ {
      proxy_pass http://backend;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static/ { proxy_pass http://backend; }

    location = /verify-email { proxy_pass http://admin/admin/verify-email; }
    location = /privacy { proxy_pass http://admin/admin/privacy; }
    location = /client-portal { proxy_pass http://admin/admin/client-portal; }

    # The static publication allowlist is explicit. All other paths fall back
    # to the existing Next.js app.
    location = / {
      root /usr/share/nginx/html;
      try_files /index.html =404;
    }

    location ~ ^/(?:articles|projects)/ {
      root /usr/share/nginx/html;
      index index.html;
      autoindex off;
      try_files $uri $uri/ =404;
    }

    location ~ ^/(?:resume(?:\.(?:zh|en))?\.html|styles\.css|resume\.css|site\.js)$ {
      root /usr/share/nginx/html;
      try_files $uri =404;
    }

    location / {
      proxy_pass http://admin;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
      proxy_set_header Host $host;
    }
  }
}
