Place your Google Play service account JSON here as `google-service-account.json`. Do **NOT** commit real credentials.
Commit only the sanitized template `google-service-account.example.json` to illustrate the schema.
